cd "containing folder (e.g. Desktop\DanielJoseph\AfricaProgramMap)"
python -m SimpleHTTPServer

Should show something like "Serving HTTP on 0.0.0.0 port 8000..."

in Chrome <localhost:8000/filename(e.g. africaIndex.html)>

